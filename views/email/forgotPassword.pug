extends base


block content
    p Hi #{firstName}
    p forgot your password ? submit a patch request with your new password and passwordConfrim to: #{url}.
    
    table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
     tbody
         tr
          td(align='left')
             table(role='presentation', border='0', cellpadding='0', cellspacing='0')
             tbody
                 tr
                  td
                     a(href=`${url}`, target='_blank') Reset Password
    p if you didn't forget tour password,please ignore this email